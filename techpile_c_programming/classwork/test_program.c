# include <stdio.h>
 
// This is a main function It gets called automatically
// Void returns null when it specify that the functions will not return anything
int main()
{
  printf("Hello everyone from the human who lives on the small \n mudball in this milkyway galaxy in this vast universe\n");
  int result =100;
  printf("output is :%d", result);
  int result = 100+200;
  printf("The result is : %d" , 100+200*2/3);
  return 0;
}
